# Deferred browser tests

The `legacy` directory contains the earlier Puppeteer and `odiff-bin`
prototype. It is not part of the current test suite, and its dependencies are
not installed by this project.

Use it as a starting point if browser-level end-to-end tests are added later.
